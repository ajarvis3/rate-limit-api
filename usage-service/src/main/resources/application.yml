spring:
  application:
    name: usage-service
  datasource:
    url: jdbc:postgresql://localhost:5432/usage_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083
