spring:
  application:
    name: usage-service
  datasource:
    url: jdbc:mysql://mysql:3306/usage_db
    username: usage_user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083
